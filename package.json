{
  "scripts": {
    "start": "concurrently npm:dev:*",
    "start:dev": "snowpack dev",
    "build": "snowpack build",
    "dev:build": "snowpack build --watch",
    "dev:server": "live-server build",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "@bundled-es-modules/axios": "^0.18.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "live-server": "^1.2.1",
    "prettier": "^2.0.5",
    "snowpack": "^2.16.0"
  }
}
